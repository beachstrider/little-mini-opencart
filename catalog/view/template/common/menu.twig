{% if categories %}
	<div class="border-b-[1px] border-[#6B7A93] py-[1px] sm:flex hidden justify-center items-center bg-[#FFF3E2] shadow-2xl">
		{% for category in categories %}
			<a href="{{ category.href }}" class="text-[10px] text-[#080E16] uppercase leading-[12px]">{{ category.name }}</a>
			<div class="px-[14px] last:hidden">⋅</div>
		{% endfor %}
	</div>
{% endif %}
